# requirements-prod.txt
# Dependencias MÍNIMAS de runtime para el despliegue de la API (FastAPI).
# Versiones tomadas del freeze validado en local (requirements.txt).
# NOTA: Azure App Service (Oryx) instala desde requirements.txt; este archivo
# debe activarse como requirements.txt en el momento del despliegue.

# --- Servidor web / entrypoint (app.main:app) ---
fastapi==0.136.3
uvicorn==0.49.0
gunicorn==23.0.0          # no está en el freeze; servidor de producción para App Service
pydantic==2.13.4

# --- Configuración ---
python-dotenv==1.2.2

# --- RAG: Azure AI Search ---
azure-core==1.41.0
azure-search-documents==12.0.0

# --- LLM: Gemini vía LangChain ---
langchain-core==1.4.0
langchain-google-genai==4.2.4

# --- Datos y forecasting en runtime (tools.py / dynamic_forecast.py) ---
pandas==3.0.3
numpy==2.4.6
scipy==1.17.1
statsmodels==0.14.6
